## R CMD check results

This is a major update (0.0.7.3 -> 0.1.0) that expands the package scope
from a DBC file reader to a complete DATASUS data access toolkit.

Main changes:

- Added `datasus_fetch()` for one-step list/download/read workflows
- Fixed SIM path template bugs (DOEXT/DOINF/DOMAT pointed to wrong FTP dirs)
- Fixed data-masking ambiguity (column names shadowing function arguments)
- Removed all `.data$` pronoun usage in favour of bare column names + `.env$`
- Replaced deprecated purrr functions
- Added `rlang` as explicit dependency
- Improved error messages with structured `cli::cli_abort()` hints
- Expanded vignettes and added a comparison article

0 errors | 0 warnings | 0 notes
